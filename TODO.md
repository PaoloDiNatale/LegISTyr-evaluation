# implement batch processing
# implement trf model
# Exit compound splitter (done)

you have created the raw df in find terms. 
call the function with both dfs
you have to change the arguments of the root function in term_finder_utils
you have to call it correctly from results_utils
you have to change the find_terms() function with the new raw_info

